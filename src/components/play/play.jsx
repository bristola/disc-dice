import { useEffect, useState } from 'react';

const Play = () => {
    const [data, setData] = useState([]);

    useEffect(() => {
      // Some code on updates
    }, []);

    const addData = (newCustomer) => {
      // Some function
    }

    return (
      <div>
        <h1>PLAY</h1>
      </div>
    );
  }
  
  export default Play;
  